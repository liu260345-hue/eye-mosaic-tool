name: Build Desktop Apps

on:
  workflow_dispatch:  # 手动触发

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: windows-latest
            name: Windows
            artifact: 视频眼部马赛克工具Web版-Windows
          - os: macos-latest
            name: macOS
            artifact: 视频眼部马赛克工具Web版-macOS

    runs-on: ${{ matrix.os }}
    name: Build ${{ matrix.name }}

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          pip install pyinstaller flask mediapipe opencv-python numpy

      - name: Download models
        run: |
          python -c "
          import urllib.request, os
          models = {
              'face_landmarker.task': 'https://storage.googleapis.com/mediapipe-models/face_landmarker/face_landmarker/float16/latest/face_landmarker.task',
              'face_detection_yunet_2023mar.onnx': 'https://github.com/opencv/opencv_zoo/raw/main/models/face_detection_yunet/face_detection_yunet_2023mar.onnx',
          }
          for name, url in models.items():
              if not os.path.exists(name):
                  print(f'Downloading {name}...')
                  urllib.request.urlretrieve(url, name)
                  print(f'{name} done')
          "

      - name: Build with PyInstaller (Windows)
        if: runner.os == 'Windows'
        run: |
          pyinstaller --noconfirm ^
            --name "视频眼部马赛克工具Web版" ^
            --console ^
            --add-data "web\templates;web\templates" ^
            --add-data "eye_mosaic.py;." ^
            --add-data "face_landmarker.task;." ^
            --add-data "face_detection_yunet_2023mar.onnx;." ^
            --hidden-import=mediapipe ^
            --hidden-import=mediapipe.tasks ^
            --hidden-import=mediapipe.tasks.python ^
            --hidden-import=mediapipe.tasks.python.vision ^
            --hidden-import=flask ^
            --hidden-import=cv2 ^
            --hidden-import=numpy ^
            --collect-all mediapipe ^
            web_launcher.py
        shell: cmd

      - name: Build with PyInstaller (macOS)
        if: runner.os == 'macOS'
        run: |
          pyinstaller --noconfirm \
            --name "视频眼部马赛克工具Web版" \
            --console \
            --add-data "web/templates:web/templates" \
            --add-data "eye_mosaic.py:." \
            --add-data "face_landmarker.task:." \
            --add-data "face_detection_yunet_2023mar.onnx:." \
            --hidden-import=mediapipe \
            --hidden-import=mediapipe.tasks \
            --hidden-import=mediapipe.tasks.python \
            --hidden-import=mediapipe.tasks.python.vision \
            --hidden-import=flask \
            --hidden-import=cv2 \
            --hidden-import=numpy \
            --collect-all mediapipe \
            web_launcher.py

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: dist/视频眼部马赛克工具Web版/
          compression-level: 9
